model Direccion {
  idDireccion  String @id @default(cuid())
  calle String
  altura  String
  barrio  String
  codigo_postal Int
  idUser String @map("idUser")

  cliente User @relation(fields: [idUser], references: [idUser])
  pedidos PedidoCab[]

  @@map("direcciones") // Necesario? 
}
