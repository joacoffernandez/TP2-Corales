model Cliente {
  idCliente        String @id @default(cuid())
  nombre String
  email     String @unique
  password  String // hashed
  telefono Int

  direcciones Direccion[] 
  mesasReservadas Mesa[]
  pedidos PedidoCab[]

  @@map("clientes")
}
